version: '3.8'

services:
  visualization:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ../output:/app/data
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
    networks:
      - dollarpunk-network

networks:
  dollarpunk-network:
    external: true
