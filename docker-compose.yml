version: '3.8'

services:
  dollarpunk-ingest:
    build: .
    volumes:
      - ./output:/app/output
      - ./.env:/app/.env:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped 